import { Card, CardContent, Grid, Typography } from "@mui/material";

const CreateTeam = () => {
    return (
        <Grid item>
            <Card>
                <CardContent>
                    <Typography>
                        Team
                    </Typography>
                </CardContent>
            </Card>
        </Grid>
    ); 
};

export default CreateTeam;